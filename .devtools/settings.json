{
  "instructions": {
    "custom": [
      "DOCUMENTATION THEME: Focus on SPIFFE/SPIRE local laptop testing with enterprise deployment paths",
      "Structure documentation: Local Setup → Testing → Enterprise Extension → Scaling",
      "Always provide working local examples first, then show enterprise adaptations",
      "Emphasize developer experience on macOS laptops with minikube clusters",
      "Include both 'laptop testing' and 'production deployment' sections in guides",
      "Use enterprise service names: user-service, payment-api, inventory-service",
      "Verify kubectl contexts before making cluster changes",
      "Follow SPIFFE ID format: spiffe://example.org/workload/service-name",
      "Show security hardening steps for production environments",
      "Always explain 'In production, you would...' for enterprise context"
    ]
  },
  "tools": {
    "preferences": {
      "kubectl_contexts": ["spire-server-cluster", "workload-cluster"],
      "dashboard_url": "http://localhost:3000/web-dashboard.html",
      "api_endpoint": "http://localhost:3000/api/pod-data"
    }
  }
}